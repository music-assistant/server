{
  "config_entries": {
    "path": {
      "label": "Path"
    },
    "content_type": {
      "label": "Content type in media folder(s)",
      "description": "The type of content to expect in the media folder(s)",
      "options": {
        "music": "[%key:common::config_entries::content_type::options::music%]",
        "audiobooks": "[%key:common::config_entries::content_type::options::audiobooks%]",
        "podcasts": "[%key:common::config_entries::content_type::options::podcasts%]",
        "sound_effects": "[%key:common::config_entries::content_type::options::sound_effects%]"
      }
    },
    "library_sync_tracks": {
      "label": "Import tracks/files into the Music Assistant library",
      "description": "Define how/if you want to import tracks/files from the filesystem into the Music Assistant Library. \nWhen not importing into the library, they can still be manually browsed using the Browse feature. \n\nPlease note that by adding a Track into the Music Assistant library, the track artists and album will always be imported as well."
    },
    "library_sync_playlists": {
      "label": "Import playlists (m3u files) into the Music Assistant library",
      "description": "Define how/if you want to import playlists (m3u files) from the filesystem into the Music Assistant Library. \nWhen not importing into the library, they can still be manually browsed using the Browse feature."
    },
    "library_sync_podcasts": {
      "label": "Import Podcasts(files) into the Music Assistant library",
      "description": "Define how/if you want to import Podcasts(files) from the filesystem into the Music Assistant Library. \nWhen not importing into the library, they can still be manually browsed using the Browse feature."
    },
    "library_sync_audiobooks": {
      "label": "Import Audiobooks(files) into the Music Assistant library",
      "description": "Define how/if you want to import Audiobooks(files) from the filesystem into the Music Assistant Library. \nWhen not importing into the library, they can still be manually browsed using the Browse feature."
    }
  },
  "manifest": {
    "description": "Play music and audiobooks stored on locally connected drives."
  },
  "errors": {
    "music_directory_not_found": "Music directory {0} does not exist."
  }
}
