{
  "config_entries": {
    "http_port": {
      "label": "HTTP Server Port",
      "description": "Port for the MSX HTTP server."
    },
    "output_format": {
      "label": "Audio Output Format",
      "description": "Audio format for streaming to MSX (mp3, aac, or flac)."
    },
    "player_idle_timeout": {
      "label": "Player Idle Timeout (minutes)",
      "description": "Unregister MSX players after this many minutes without activity."
    },
    "show_stop_notification": {
      "label": "Show notification before closing player",
      "description": "Show confirmation dialog on MSX when stopping playback from MA."
    },
    "group_stream_mode": {
      "label": "Stream Delivery Mode",
      "description": "How audio reaches the TVs. 'MA Streamserver' redirects TVs directly and is recommended. 'Independent' proxies a separate local ffmpeg stream for compatibility. Redirect falls back to Independent automatically when a direct URL cannot be resolved.",
      "options": {
        "redirect": "MA Streamserver (recommended)",
        "independent": "Independent local proxy (compatibility)"
      }
    },
    "include_content_length": {
      "label": "Include estimated Content-Length",
      "description": "Include an estimated Content-Length on MP3 and AAC streams served by the local Independent proxy. Disable this if a TV rejects or truncates estimated-length streams. This does not control Music Assistant Streamserver responses."
    }
  },
  "manifest": {
    "description": "Stream music to Smart TVs via Media Station X"
  },
  "errors": {
    "player_offline": "MSX TV {0} is offline."
  }
}
