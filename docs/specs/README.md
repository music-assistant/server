# Feature Specs

This directory contains one-page specs for new features.
Workflow:

1. Copy `docs/specs/TEMPLATE.md` to `docs/specs/<feature>.md`
2. Fill in the spec — problem, solution, files affected
3. Load it into OpenCode/Claude Code — the agent implements from the spec
4. Review + Pre-Commit + Test
5. Push + PR
